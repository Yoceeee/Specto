<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/main_color">

    <TextView
        android:id="@+id/titleSearch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="15dp"
        android:layout_marginTop="40dp"
        android:text="Keresés"
        android:textColor="@color/searchTitle"
        android:textSize="28sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.appcompat.widget.SearchView
        android:id="@+id/searchView"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginHorizontal="16dp"
        android:layout_marginTop="16dp"
        android:background="@drawable/search_bar"
        app:iconifiedByDefault="false"
        app:queryHint="Filmek, sorozatok..."
        app:queryBackground="@null"
        app:submitBackground="@null"
        app:layout_constraintTop_toBottomOf="@id/titleSearch" />

    <HorizontalScrollView
        android:id="@+id/chipScroll"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:clipToPadding="false"
        android:paddingHorizontal="16dp"
        android:scrollbars="none"
        app:layout_constraintTop_toBottomOf="@id/searchView">

        <com.google.android.material.chip.ChipGroup
            android:id="@+id/chipGroup"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            app:singleLine="true">

            <com.google.android.material.chip.Chip
                android:id="@+id/newContentChipButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:checkable="true"
                android:clickable="true"
                android:focusable="true"
                android:text="Új"
                android:textColor="@color/searchBar"
                app:chipBackgroundColor="@drawable/search_filter" />

            <com.google.android.material.chip.Chip
                android:id="@+id/bestContentChipButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:checkable="true"
                android:clickable="true"
                android:focusable="true"
                android:textColor="@color/searchBar"
                android:text="Legjobb"
                app:chipBackgroundColor="@drawable/search_filter" />

            <com.google.android.material.chip.Chip
                android:id="@+id/movieChipButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/searchBar"
                android:checkable="true"
                android:clickable="true"
                android:focusable="true"
                android:text="Filmek"
                app:chipBackgroundColor="@drawable/search_filter" />

            <com.google.android.material.chip.Chip
                android:id="@+id/seriesChipButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/searchBar"
                android:checkable="true"
                android:clickable="true"
                android:focusable="true"
                android:text="Sorozatok"
                app:chipBackgroundColor="@drawable/search_filter" />

        </com.google.android.material.chip.ChipGroup>
    </HorizontalScrollView>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="12dp"
        android:clipToPadding="false"
        android:paddingBottom="90dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/chipScroll"
        tools:listitem="@layout/content_card" />

</androidx.constraintlayout.widget.ConstraintLayout>